[描述]
fmt.Printf("%p\n", s) 和 fmt.Printf("%p\n", &s) 有什么区别
fmt.Printf("%p\n", s)：这会打印变量 s 的值的地址。在这种情况下，s 是一个指针类型的变量，它存储了一个内存地址。%p 格式化动词用于打印指针的十六进制地址。

fmt.Printf("%p\n", &s)：这会打印变量 s 本身的地址。在这种情况下，&s 表示 s 的地址，%p 格式化动词用于打印该地址的十六进制表示。